#!/usr/bin/env lunch

add_command("spinserver --spatosc BasicTranslator osc.udp://localhost:10001 --scene-id pyospat",
identifier="spinserver")
add_command("pd-extended -noaudio viaSpin.pd", identifier="pd")
